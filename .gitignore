# Python
__pycache__/
*.pyc
.pytest_cache/
venv/
*.egg-info/

# Data — never commit (CRSP licensing; also just large)
data/raw/*
data/interim/*
data/processed/*
!data/raw/.gitkeep
!data/interim/.gitkeep
!data/processed/.gitkeep

# OS
.DS_Store

# Model checkpoints and resumable-run state (regenerated by step1-3;
# includes QUICK_MODE's *_quick-suffixed variants)
reports/models/*.pt
reports/models/*.ckpt
data/processed/_classical_checkpoint*.json
data/processed/_arma_orders*.json
data/processed/_tft_X*.npy
data/processed/_tft_Y*.pt
data/processed/_tft_meta*.pt

# Downloaded Fama-French/Carhart factors — small enough to keep locally,
# but re-downloadable, not source
data/raw/ff_carhart_daily.csv